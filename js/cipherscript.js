var sentence = prompt("Please enter a sentence.");
console.log(sentence);

var capletters = function(){
  alert(first, last);
};
var first = sentence.charAt(0).toUpperCase();
var last = sentence.charAt(sentence.length-1).toUpperCase();
capletters();

var capreverse = function(){
  alert(last, first);
};
capreverse();

var capandreverse = function(){
  alert(first + last);
  alert(sentence + last + first);
};
capandreverse();

var letterdivide = function(){
  var sum = sentence.length;
  var index = sum / 2;
  var display = sentence.charAt(index);
  alert(display + sentence);
};
letterdivide();

var str= String(letterdivide);
alert(str);
var reverse = dunction(){
  alert(str.split("").reverse().join(""));
};
reverse();

var middle = sentence.substring(1, sentence.length-1);
[firstcap, lastcap = [lastcap, firstcap];

alert((firstcap + lastcap) + (lastcap + firstcap));
